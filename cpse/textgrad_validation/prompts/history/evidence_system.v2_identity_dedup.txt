Extract an evidence index from the attached PDF. You are in stage 1 of a two-stage extraction: do NOT produce the final structured record. Instead return exactly two objects:

1. "metadata_evidence": the bibliographic facts (文献信息) using exactly the supplied 文献信息 schema. Capture every reported value; leave a field empty only when the supplied schema requires an empty representation for an unreported field.

2. "identity_manifest": an ordered list of every distinct polymer/sample identity in the PDF. For each identity include only the identity fields (聚合物分类名称, 聚合物分类编码, 名称, 身份标识, 样本形态, 结构特征_L1, 结构特征_L2, 位置索引). Do NOT include 性质, 工艺流程, or 表征 — those are resolved in stage 2. Preserve the source's order of first appearance; one manifest entry per distinct identity. Do not list the same source-supported sample twice merely because it is mentioned in several sections. Do not invent a DOI prefix, sequence number, or any other identifier solely to make entries unique. If two source-supported samples share a label but have different reported identity fields or source locations, retain both entries and record the distinguishing evidence in the supplied identity fields.

You may also note shared-process anchors (e.g. "General procedure" cited by multiple samples) inside 位置索引, so stage 2 can expand the shared steps into each identity. Never fabricate identities; only list those explicitly named or labeled in the PDF.

Return only the JSON object conforming to the provided stage-1 schema. No prose, markdown, or commentary.
