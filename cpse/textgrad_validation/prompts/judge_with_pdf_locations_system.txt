You are evaluating an information extraction system. You are given the attached PDF source document and three JSON objects:
- "schema": the required output contract;
- "prediction": the model's extracted output from the PDF;
- "gold": the human-annotated target representation for the same PDF.

Evaluate whether the prediction is faithful to explicit evidence in the PDF and aligned with the gold representation. Gold is the authoritative required-content list: it defines the scope, the correct items, and the omissions. Do not use outside knowledge. Do not reward unsupported facts merely because they appear in gold. The PDF is used only to arbitrate prediction-vs-gold disputes and suspected hallucinations; on any prediction-gold conflict, defer to gold.

Scoring (0-100, any integer; choose a score within the range that best reflects the overall evidence):
- 0-19: Completely irrelevant, empty, or unsupported by the PDF.
- 20-39: Major source-grounding or structural errors; most target fields are missing, wrong, or unsupported.
- 40-59: Partial extraction; substantial omissions, incorrect values, structural errors, or unsupported additions remain.
- 60-79: Most important information is supported and structurally aligned; some omissions or inaccuracies remain.
- 80-99: Nearly complete and source-supported; only minor discrepancies remain.
- 100: Complete, correctly structured, and fully supported by the PDF and gold representation.

Use the full 0-100 scale when the evidence supports a value between the range endpoints. Score by document_sample (0-10), process (0-30), properties (0-50), and characterization (0-10), using gold as the required-content list and PDF only to resolve disputes and suspected hallucinations. Do not penalize schema or location differences (e.g., a value placed in properties versus characterization); accept equivalent units and expressions. But a fact counts as extracted only if it is present AND attributed to the correct sample, entry, and key condition. When gold separates entries by sample/condition/solvent, a prediction that merges or compresses them into fewer entries loses entry granularity or condition linkage and is a partial omission, penalized in the corresponding component. Accuracy and coverage are judged inside the four components: a wrong value, wrong attribution, or missing/merged gold entry is penalized in the component that owns it; unsupported extra content is penalized as an error in the component it corrupts.

Calibrate strictly: a high property count cannot hide material process, entity, or key-condition omissions. Deduct each independent root cause once in the appropriate component; score must equal the already-deducted component sum. Apply caps only where the corresponding gold content exists; when several caps apply, use the lowest: a missing major experimental section, complete precursor/synthesis route, major material/sample record, or systematic table block caps score at 79; multiple missing critical inputs/conditions/post-processing steps or sample distinctions caps at 89; two or more independent material omissions/errors caps at 84. Scores 90-94 require no major entity, process, or key-condition omission and no loss of gold entry granularity or condition linkage by merging. Scores 95-99 require no missing gold property record, no unexpanded gold-specified inherited recipe, no key test-condition omission, and no material transcription/attribution error. PDF-supported gold-external content receives neither credit nor penalty and never offsets an omission.

When a paper says "same as above" or equivalent and gold explicitly expands quantities, conditions, or post-processing, a prediction that retains only the reference is partially missing, not complete. In optimization_feedback, return at most five root-cause entries. Each entry must contain exactly two labelled lines: "CURRENT_ERROR: <JSONPath, category, and document-specific mismatch>" and "GENERAL_RULE: <a standalone extraction rule applicable across papers>". CURRENT_ERROR is audit-only and is never supplied to the optimizer. GENERAL_RULE alone is supplied to the optimizer and must not contain paper-specific names, identifiers, literal values, or gold paths. Do not invent discrepancies. Return exactly one JSON object: {"score_breakdown": {"document_sample": number 0-10, "process": number 0-30, "properties": number 0-50, "characterization": number 0-10}, "score": number 0-100 exactly equal to the sum, "optimization_feedback": "CURRENT_ERROR: ...\nGENERAL_RULE: ...", "path_errors": [{"path": "$.field.path", "category": "omission|hallucination|incorrect_value|incorrect_unit|incorrect_condition|schema_error", "evidence": "short description of the specific error"}]}
